# Basis-URL des Ace-Backends fuer das Chat-Widget (siehe src/layouts/Base.astro).
# Production: LEER lassen (Standard) - das Widget laeuft same-origin, Caddy
# proxied /static/widget.js + /tenants/vision-tennis/channels/webchat/* direkt
# auf ace-core (siehe infra/Caddyfile.homepage). Kein Cross-Origin-Request.
# Lokal: auf den lokal laufenden ace-core-Container zeigen, z.B.:
# PUBLIC_ACE_API_BASE=http://localhost:8010
PUBLIC_ACE_API_BASE=
